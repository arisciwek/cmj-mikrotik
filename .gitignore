# SSH / deploy keys
id_rsa
id_ed25519
*.pem
*.key

# Git credentials (repo-local, jangan pernah di-commit)
.git-credentials

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Backup binary RouterOS (berisi kredensial, jangan pernah di-commit)
*.backup

# WireGuard peer configs (private key + PSK, jangan pernah di-commit)
wg-peers/
wg-peers/*.conf
wg-peers/*.png
